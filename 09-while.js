/* Ciclo While
    Nos permite repetir linea de codigo mientras una condicion sea verdadera.

*/

const num1 = parseInt(prompt('ingresa un número'));

while(NAN(num1)) {
    const num1 = parseInt(prompt('ingresa un numero'));
}